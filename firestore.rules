rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Starter rules: any signed-in user can read and write.
    // Harden before public launch (see docs/DEPLOYMENT.md).
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
